{
  "name": "@codeplaydata/tesa",
  "version": "0.1.0",
  "description": "Tools for easy spatial analysis is the most beloved project that deals with geojson objects of city locations. #Frictionless_Data ;)",
  "main": "index.js",
  "type": "module",
  "author": "Pedro Paulo dos Santos (dr2p) - https://github.com/dr2pedro",
  "license": "Apache 2.0",
  "scripts": {
    "build": "tsc && cp LICENSE ./dist/LICENSE && cp CHANGELOG.md ./dist/CHANGELOG.md &&   cp README.md ./dist/README && cp package.json ./dist/package.json",
    "publish": "npm publish ./dist --access=public",
    "test": "npm run build && node --test --trace-warnings ./dist",
    "test-watch": "npm run build && node --test --trace-warnings --watch ./dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/codeplaydata/tesa.git"
  },
  "bugs": {
    "url": "https://github.com/codeplaydata/tesa/issues"
  },
  "homepage": "https://github.com/codeplaydata/tesa#readme",
  "keywords": [
    "geocoding",
    "client",
    "geolocation",
    "maps"
  ],
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.16.1",
    "typescript": "^5.0.4"
  }
}
